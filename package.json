{
  "name": "sendgrid-mcp",
  "version": "1.0.0",
  "description": "Send emails and interact with SendGrid",
  "private": true,
  "type": "module",
  "bin": {
    "sendgrid-mcp": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && chmod +x build/index.js", // Keep build
    "watch": "tsc --watch",                  // Optional: Keep watch
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js", // Keep test
    "start": "node build/index.js"          // *** ADD START SCRIPT BACK ***
    // Remove "prepare" script
    // Remove "inspector" unless needed
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.10.0",     // *** USE PINNED VERSION 1.9.0 ***
    "@sendgrid/client": "^8.1.4",
    "@sendgrid/helpers": "^8.0.0",
    "@sendgrid/mail": "^8.1.4",
    "express": "^4.18.2",                    // *** ADD EXPRESS BACK ***
    "dotenv": "^16.4.7"                      // Keep dotenv if needed
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.11.24",
    "@types/express": "^4.17.17",           // *** ADD @types/express BACK ***
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": "20.x"                          // *** ADD ENGINES BACK ***
  }
}
